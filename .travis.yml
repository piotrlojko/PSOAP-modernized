language: python
python:
  - "3.12"

install:
  - pip install -r requirements.txt
  - python setup.py build_ext --inplace
  - pip install -e .

before_script:
  - "export DISPLAY=:99.0"
  - "sh -e /etc/init.d/xvfb start"
  - sleep 3

script: pytest
